version: "3.8"
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: peakconnect
      POSTGRES_USER: peakconnect_user
      POSTGRES_PASSWORD: localdevpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: